# Token Guardian Prometheus Service Discovery
# This file defines service discovery targets for Prometheus

# Token Guardian Daemon
- targets:
    - localhost:9090
  labels:
    service: tokenguardian
    component: daemon
    env: production
